/**
 * Ori Back Office — persistent operations dashboard (server-backed).
 * Replaces browser-only admin storage with the unified back office shell.
 */

import { BackOfficeApp } from "../components/backoffice/BackOfficeApp";

export function AdminPage() {
  return <BackOfficeApp />;
}
