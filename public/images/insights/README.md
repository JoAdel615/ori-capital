# Optional: Insight thumbnails

Place image files here (e.g. `founder-desk.jpg`, `storefront.jpg`) to use as post thumbnails. Reference them in `src/data/blog.ts` by path, e.g. `/images/insights/founder-desk.jpg`. The app currently uses SVG placeholders from `src/assets/insights/` when no image path is set per post.
